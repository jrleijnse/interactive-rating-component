const numberList = document.querySelector(".number-list");
const selectedNumber = undefined;
// addEventLi => list
// e.target.dataset.rate
// addC
// store number => seleNum = 1

const submitRating = function (e) {
  // check if sNum != und
  // === und => alerrt ... select a number

// != undefined => submit() => reste()
  // send the request
};

const selectRating = function ()